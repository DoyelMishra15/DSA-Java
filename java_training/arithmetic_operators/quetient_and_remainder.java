package java_training.arithmetic_operators;

public class quetient_and_remainder {
    public static void main(String args[]){
        int num = 10, divisor = 3;
        int quotient = num / divisor;
        int remainder = num % divisor;
        System.out.println("Number: " + num + ", Divisor: " + divisor + ", Quotient: " + quotient + ", Remainder: " + remainder);
    }
}
